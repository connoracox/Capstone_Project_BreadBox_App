<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <img class="nav_logo" src="../static/images/bread.png" alt="bread">
    <a class="navbar-brand" href="{{ url_for('main.index') }}">BreadBox</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="{{ url_for('main.index') }}">Home</a>
        <a class="nav-link" href="{{ url_for('main.about') }}">About</a>
        {% if current_user.is_active %}
        {% if current_user.vendor == 'vendor' %}
        <a class="nav-link" href="{{ url_for('social.user', username=current_user.username)}}">Profile</a>
        <a class="nav-link" href="{{ url_for('social.post') }}">Post Boxes</a>
        {% endif %}
        {% if current_user.vendor == 'user' %}
        <a class="nav-link" href="{{ url_for('social.explore_food') }}">Explore Food</a>
        <a class="nav-link" href="{{ url_for('social.my_boxes') }}">My Boxes</a>
        {% endif %}
        <a class="nav-link" href="{{ url_for('auth.sign_out') }}">Sign Out</a>
        {% else %}
        <a class="nav-link" href="{{ url_for('auth.signin') }}">Sign In</a>
        <a class="nav-link" href="{{ url_for('auth.register') }}">Register</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>