{% extends "base.jinja" %}

{% block content %}
<h2 class="page_title">{{ current_user.username.title() }}, Congrats on placing your order!</h2><br><br>
<div class="order_box">
    <h4 class="ord_conf"><small>Order Confirmation Number: </small><br><b>{{ random.randint(1,100000) }}</b></h4>
    <br><br>
    {# <p> Buyer:{{ (user.buyer.buyer) }}</p> #}
    <img class="qrcode" src="../static/images/qr-code.png" alt="QRcode">
    <p class="instructions"><strong>Be sure to show your Order Confirmation Number and QR Code to a staff member when you arrive!</strong></p>
    <form action="{{ url_for('main.index') }}" method="POST">
            <input type="submit" class='btn2' value="Confirm Pick-Up">
    {# {% for buyer in buyer %}
        <p>{{ buyer }} reserved: 1 box!</p> 
    {% endfor %} #}
</div>

{% endblock %}